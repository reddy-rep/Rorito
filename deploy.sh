Mount a drive to the remote machine (where the shell script is located) on the machine hosting the Informatica Cloud Secure Agent.
The shell script will now be accessible to the agent using standard path syntax (example: /remote/usr/local/).

In the Command Task enter the entire path and the shell script name.

Example
/remote/usr/local/test.sh
